name: Publish flutter_map_sld_io to pub.dev

on:
  push:
    tags:
      - 'flutter_map_sld_io-v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish flutter_map_sld_io
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      environment: pub.dev
      working-directory: packages/flutter_map_sld_io
